---
id: {{UUID}}
requirements-id: {{REQUIREMENTS_ID}}
---

<!-- auto:start — regenerated by mo-apply-impact; do not edit this section -->

<!-- Total auto-block budget across the three sections below: ≤ 10 entries,
     ≤ 2 lines each. The auto block is loaded into every chain primer —
     extra lines are paid repeatedly. Off-topic skills/rules are omitted
     entirely; the chain can still discover them by listing
     `.claude/skills/` and `.claude/rules/` if it needs to. -->

## Skills

<!-- Skills that are likely to be consulted while implementing this cycle's
     Goals. Each entry: `- <name>: <one-line reason>; path: .claude/skills/<name>/SKILL.md`. -->

## Rules

<!-- Rules that constrain this cycle's implementation. Same format as Skills.
     Each entry: `- <name>: <one-line reason>; path: .claude/rules/<name>.md`. -->

## Load on demand

<!-- Skills/rules that exist in the project and may apply if a related concern
     surfaces during brainstorming or review, but aren't required up front for
     the active Goals. Each entry: `- <name>: <when to load>; path: ...`.
     Skip the section entirely when no such tangential entries exist. -->

<!-- auto:end -->

## GIT BRANCH

<!-- The single feature branch that will carry this feature's implementation
     commits. Required before /mo-plan-implementation will launch.

     One branch per feature — if you list multiple, the millwright will warn
     you and ask you to pick one. If a feature genuinely needs coordinated
     work across multiple branches (e.g. a monorepo with separate frontend
     and backend branches), split it into two features instead — add one
     todo item per branch so each has its own review scope and audit trail.

     Format (one line, no bullet, no quotes):
       feat/<scope>/<name>

     main / master are refused. If this section is empty at stage 3,
     /mo-plan-implementation will either prompt you in chat or ask you
     to fill it in here and re-run. -->

## Overseer Additions

<!-- Anything below this marker is preserved verbatim on regeneration.
     Add custom prompts, MCP invocations, project-specific instructions, etc. -->
