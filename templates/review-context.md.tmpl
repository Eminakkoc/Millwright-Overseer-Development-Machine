---
id: {{UUID}}
requirements-id: {{REQUIREMENTS_ID}}
feature: {{FEATURE}}
---

# Review context — {{FEATURE}}

Compact context bundle for the stage-6 brainstorming review session. When this snapshot covers a topic, the chain does NOT need to re-load the canonical files. When a gap surfaces while addressing a finding, drop into the on-demand references at the bottom.

> **Snapshot caveat:** The body of this file is a point-in-time snapshot from
> when `/mo-review` was invoked. If `/mo-update-blueprint` runs mid-session,
> `review.sh sync-refs` re-points the `requirements-id` frontmatter but does
> NOT regenerate the body. When goals or implemented surface look inconsistent
> with the canonical files, trust `overseer-review.md` and
> `blueprints/current/requirements.md` over this snapshot.

<!-- mo:sync-marker — managed by review.sh sync-refs; do not hand-edit -->

<!-- Generated artifact — written by /mo-review just before the brainstorming review
     session is invoked. Cleaned up by /mo-complete-workflow and /mo-abort-workflow.
     Treat overseer-review.md and the canonical blueprint files as the source of
     truth; this is a snapshot to spare repeated reads. -->

## Active scope

<!-- Fill from progress.md (active.branch, active.base-commit). -->

- branch: <branch>
- base-commit: <sha>
- commit range: `<base-commit>..HEAD` — re-run `git rev-list <base>..HEAD` if exact list is needed

## Goals (this cycle)

<!-- 5–20 lines. Excerpt from blueprints/current/requirements.md `## Goals (this cycle)`.
     This is the contract the implementation should satisfy; findings are anchored
     to drift from it. -->

## Implemented surface

<!-- High-level grouping of what's in `base-commit..HEAD`. Two short lists:

     - Changed areas: a few bullets naming the modules/paths the diff touched
       (e.g., `src/payments/webhook.ts`, `migrations/2026-04-25-add-tx-table.sql`).
       Do not paste full diffs; the chain runs `git diff` itself when needed.
     - Diagrams: list every file under implementation/diagrams/ with a one-line
       purpose. The chain may consult specific diagrams for a finding. -->

## Open findings (snapshot)

<!-- One line per finding, in the order they appear in overseer-review.md:
     - `IR-NNN` (<severity>): <summary line>
     The chain re-reads overseer-review.md as the canonical state — this list is
     a cheat sheet, not the source of truth. Refresh it on `go again` if findings
     change materially. -->

## On-demand canonical files

If this snapshot is missing context, read these directly:

- `millwright-overseer/workflow-stream/{{FEATURE}}/implementation/overseer-review.md` — canonical findings (read on every loop trip; the chain re-reads on `go again`)
- `millwright-overseer/workflow-stream/{{FEATURE}}/blueprints/current/requirements.md` — full goals / planned / non-goals
- `millwright-overseer/workflow-stream/{{FEATURE}}/blueprints/current/config.md` — full skills/rules + GIT BRANCH + Overseer Additions
- `millwright-overseer/workflow-stream/{{FEATURE}}/blueprints/current/primer.md` — the original stage-3 launch primer (rotated if blueprint was refreshed)
- the active cycle's `summary.md` (under `millwright-overseer/quest/<active-slug>/`; the slug is recorded in `millwright-overseer/quest/active.md`) — read `## Cross-cutting constraints` and `## Feature: {{FEATURE}}` first
